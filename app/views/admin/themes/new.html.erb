<header class="sub-header">
  <ul class="menu">
    <li class="title"><h4>Themes</h4></li>
  </ul>
</header>

<section>
  <%= form_for [:admin, @theme] do |f| %>
    <%= f.file_field :file %>
    <%= f.submit "Confirm" %>
  <% end %>
</section>
