<header class="sub-header col-md-12">
  <ul class="menu">
    <li class="title"><h4>Posts</h4></li>
    <li class="item"><%= link_to "Adicionar Novo", new_admin_post_path %></li>
  </ul>
</header>

<section class="col-md-11">
  <table class="admin table col-md-12">
    <th>Título</th>
    <th>Author</th>
    <th>Date</th>

    <% @posts.each do |post| %>
      <tr>
        <td class="col-md-4"><%= link_to post.title, edit_admin_page_path(post) %></td>
        <td class="col-md-2"><%= post.author%></td>
        <td class="col-md-2"><%= post.created_at.strftime("%d/%m/%Y")%></td>
      </tr>
    <% end %>
  </table>
</section>

